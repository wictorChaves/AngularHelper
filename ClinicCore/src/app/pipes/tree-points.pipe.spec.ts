import { TreePointsPipe } from './tree-points.pipe';

describe('TreePointsPipe', () => {
  it('create an instance', () => {
    const pipe = new TreePointsPipe();
    expect(pipe).toBeTruthy();
  });
});
